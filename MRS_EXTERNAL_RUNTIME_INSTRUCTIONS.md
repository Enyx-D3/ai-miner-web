# DEPRECATED — External MRS Endpoint

As of V9.0.5, the web app no longer requires `NEXT_PUBLIC_BRAIN2_MRS_ENDPOINT` for its MRS semantic/hard-residual model lane. The browser runtime uses WebLLM + WebGPU with `Qwen2.5-0.5B-Instruct-q4f16_1-MLC`.

See `WEBLLM_MRS_INTEGRATION_V9_0_5.md`.
